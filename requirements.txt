# Async HTTP
aiohttp>=3.9.0
aiofiles>=23.0.0

# Jira API
jira>=3.6.0

# Document extraction — MarkItDown handles PPTX, PDF, DOCX, images, etc.
markitdown[all]>=0.0.1

# PDF page metadata peek (triage cheap_peek — optional)
PyMuPDF>=1.23.0

# Text processing
langdetect>=1.0.9

# LLM / Embeddings
openai>=1.30.0

# Vector store — LangGraph ecosystem
langgraph>=0.2.0
langchain-core>=0.2.0

# Persistent vector store backends (optional — pick one)
# langchain-chroma>=0.1.0
# faiss-cpu>=1.8.0

# Parquet output (optional — only needed for --fmt parquet)
pyarrow>=15.0.0

# Config
python-dotenv>=1.0.0
